# GitHub Workflow - Инструкция по работе с репозиторием

## 📦 Репозиторий
**URL**: https://github.com/Sjunja/samarqand-spring-school

## 🔑 Аутентификация
Git настроен с сохранением учетных данных. При следующем push/pull токен будет использован автоматически.

---

## 🚀 Рабочий процесс

### Сделать изменения и закоммитить

```bash
cd "e:\Samarqand School site - 20-01-2026"

# Посмотреть статус
git status

# Добавить все изменения
git add .

# Или добавить конкретные файлы
git add путь/к/файлу

# Создать коммит
git commit -m "Описание изменений"
```

### Отправить на GitHub

```bash
git push
```

### Получить обновления с GitHub

```bash
git pull
```

---

## 📝 Примеры коммитов

### Хорошие примеры:
```bash
git commit -m "Add payment confirmation feature"
git commit -m "Fix registration form validation"
git commit -m "Update Russian translations"
git commit -m "Optimize images for faster loading"
```

### Плохие примеры:
```bash
git commit -m "changes"           # ❌ Слишком общее
git commit -m "fix bug"           # ❌ Какой баг?
git commit -m "update"            # ❌ Что обновили?
```

---

## 🌿 Работа с ветками

### Создать новую ветку для фичи

```bash
# Создать и переключиться на новую ветку
git checkout -b feature/payment-integration

# Сделать изменения, закоммитить
git add .
git commit -m "Add payment integration"

# Отправить ветку на GitHub
git push -u origin feature/payment-integration
```

### Вернуться на main

```bash
git checkout main
```

### Слить ветку в main

```bash
git checkout main
git merge feature/payment-integration
git push
```

---

## 🔄 Автоматический деплой на Cloudflare Pages

После каждого `git push` на ветку `main`:
1. ✅ Cloudflare автоматически обнаружит изменения
2. ✅ Запустит сборку проекта
3. ✅ Задеплоит на https://samarqand-school.pages.dev

Проверить статус деплоя можно в Cloudflare Dashboard → Workers & Pages → samarqand-school → Deployments

---

## ⚠️ Важные файлы (не коммитить!)

Следующие файлы уже в `.gitignore` и **НЕ ДОЛЖНЫ** попадать в Git:

- `node_modules/` - зависимости (очень большие)
- `.env` - секретные ключи
- `dist/` - собранные файлы (генерируются при сборке)
- `*.log` - логи
- `.DS_Store`, `Thumbs.db` - системные файлы

---

## 🆘 Решение проблем

### Конфликт при pull

```bash
# Сохранить локальные изменения
git stash

# Получить обновления
git pull

# Вернуть локальные изменения
git stash pop

# Если есть конфликты, исправьте их вручную, затем:
git add .
git commit -m "Resolve merge conflicts"
```

### Отменить последний коммит (не отправленный на GitHub)

```bash
# Отменить коммит, но сохранить изменения
git reset --soft HEAD~1

# Отменить коммит и изменения
git reset --hard HEAD~1
```

### Посмотреть историю коммитов

```bash
git log --oneline -10
```

### Посмотреть изменения в файле

```bash
git diff путь/к/файлу
```

---

## 🔗 Полезные ссылки

- **GitHub Repository**: https://github.com/Sjunja/samarqand-spring-school
- **Cloudflare Pages**: https://dash.cloudflare.com
- **Live Site**: https://samarqand-school.pages.dev (после деплоя)

---

## 📞 Поддержка

Если возникли проблемы с Git:
1. Проверьте `git status`
2. Прочитайте сообщение об ошибке
3. Поищите решение в этом файле
4. Или обратитесь к разработчику
